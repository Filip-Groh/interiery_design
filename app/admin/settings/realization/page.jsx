import React from 'react'
import SettingsMenu from '@/app/components/settings/menu'

const RealizationSettings = () => {
    return (
        <SettingsMenu activeTabName="Realization">

        </SettingsMenu>
    )
}

export default RealizationSettings